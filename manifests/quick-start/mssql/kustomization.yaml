apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../base

resources:
  - argo-mssql-config-secret.yaml
  - mssql-deployment.yaml
  - mssql-service.yaml

patchesStrategicMerge:
  - overlays/workflow-controller-configmap.yaml